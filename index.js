// multiply nums to equal to 62
let num1 = 2;
let num2 = 31;
const multiply = num1 * num2;

// generate random int > 0
let random = Math.floor(Math.random() * 10) + 1;

// calculate remainder = 4
let num3 = 24;
let num4 = 5;
const mod = num3 % num4;

// return 20 as highest number in set
let numList = (1, 2, 3, 4, 20);
const max = Math.max(numList);

// alternative
// let numList = [1, 2, 3, 4, 20];
// const max = Math.max(...numList);